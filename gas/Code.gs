// ============================================
// KicksWrap 発注自動化 — メインスクリプト
// ============================================
//
// /dev コマンドで Phase ごとに実装していきます。
// 最初は中身が空の関数だけ並んでいます。
//

// ──────────────────────────────────────────
// Phase A: 在庫チェック
// 在庫管理スプシを読み取り、閾値を下回った商品を見つける
// ──────────────────────────────────────────

function checkInventory() {
  // A-1: スプレッドシートからデータ取得
  // → /dev Phase A で実装

  // A-2: 商品ごとに閾値チェック
  // → /dev Phase A で実装

  // A-3: 閾値を下回った商品があれば通知へ
  // → notify(alerts) を呼ぶ
}


// ──────────────────────────────────────────
// Phase B: Slack通知
// 発注検討アラートをSlackに送る
// ──────────────────────────────────────────

function notify(alerts) {
  // B-1: アラート情報を整形
  // → /dev Phase B で実装

  // B-2: Slack Webhook にPOST
  // → /dev Phase B で実装
}


// ──────────────────────────────────────────
// Phase D: Claude APIで発注判断（後から追加）
// 発注者.mdをsystem_promptにして発注下書きを生成する
// ──────────────────────────────────────────

// function generateDraft(inventoryData) {
//   // D-1: GitHubから発注者.mdを取得
//   // D-2: Claude APIに送信
//   // D-3: レスポンスをパースして発注下書きを生成
//   // → /dev Phase D で実装（APIキー発行後に有効化）
// }


// ──────────────────────────────────────────
// Phase E: 月次レポート（後から追加）
// ──────────────────────────────────────────

// function monthlyReport() {
//   // E-1: 発注ログを集計
//   // E-2: Claude APIで傾向分析
//   // E-3: Notionにレポートページ作成
//   // → /dev Phase E で実装
// }
